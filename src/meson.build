PLUGINS_INSTALL_DIR = join_paths(libdir, 'budgie-desktop',
	'plugins')

install_data(
    'BudgiePiTempMonitor.plugin',
    'budgie_pi_temp_monitor.py',
    install_dir: join_paths(PLUGINS_INSTALL_DIR, application_id)
)
